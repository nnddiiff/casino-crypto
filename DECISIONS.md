# Decisions — Crypto Casino (Vibe-Code Challenge)

Журнал зафиксированных решений. Одна строка на решение: что + кратко почему.
Правило: зафиксированное не перерешиваем. Что ещё открыто — внизу.

## Участие
- Идём: да, в senior-рамке — не junior-аудишн; цель сдачи попасть на радар основателя TrueMind как равный, артефакт в портфолио и разведка Web3 как бонус.

## Стек и сеть
- Сеть: **Base Sepolia** — EVM (надёжная AI-кодогенерация + экономия токенов), быстро/дёшево, лучший paymaster+AA, есть Pyth Entropy, считается «Ethereum testnet» (L2). В README объяснить выбор явно.
- Деньги: **нативный тестовый ETH + кран в контракте** — нет деплоя токена, нет approve, простейшая петля deposit/withdraw.
- Случайность: **Pyth Entropy** — проверяемая on-chain, дешевле/быстрее Chainlink VRF, задеплоена на Base Sepolia, есть готовый пример под Foundry. Откат: commit-reveal.
- Инструменты: **Foundry** (контракт: write/test/deploy/verify) + **thirdweb v5** (фронт, кошелёк, AA, sponsorGas).
- Фронт-фреймворк: **Next.js 16 (App Router) + TypeScript**, src-dir, alias `@/*`. Почему: Vercel-native деплой без конфига, shadcn/ui и thirdweb максимально документированы под Next → меньше итераций AI; кошельковые хуки client-only закрываются провайдером-обёрткой. Внимание: это Next **16** (не 15) — ломает часть привычных API, сверяться с `web/node_modules/next/dist/docs/` (предупреждение в `web/AGENTS.md`).
- Закреплённые версии каркаса (воспроизводимость): solc 0.8.28 (evm cancun, optimizer 200), forge-std 1.16.1, OpenZeppelin 5.6.1, thirdweb 5.120, Tailwind v4, shadcn style `base-nova`.
- Сборка контракта: примитивы из **OpenZeppelin** (`ReentrancyGuard`, `Ownable`), интеграцию Pyth — из официального `coin_flip`-примера; логику Limbo (выплата, проверка, edge, банк/кран) **пишем сами**. Целый казино-контракт не форкаем — унаследованные баги с деньгами + теряется понимание для Loom/«hardest unknown».
- Онбординг: thirdweb account abstraction + безгазовый вход — **стретч**; MetaMask — гарантированная страховка.
- Дизайн/UI: **Claude Code + shadcn/ui** (Tailwind) под конкретный референс (Stake/Shuffle/BC.Game) — один инструмент, бесплатно; v0.dev не берём (скудный free-лимит + второй тул, при референсе качество то же). Фронт/полировка — приоритет; «вау» = анимированная центральная цифра множителя + тёмная тема с ярким акцентом + провабли-фейр-бейдж + mobile-first.

## Игра
- **Limbo** — игрок задаёт целевой множитель, генерируется случайный, выигрыш если ≥ цели. Один розыгрыш, простейший контракт, edge прозрачен (вероятность = RTP/T). Реальная игра (Hacksaw/Stake).

## Отличие (дифференциатор)
- **Дашборд самоаудита**: live edge/RTP/hold + ссылка на пруф случайности к каждой ставке + факт банка казино против теории. Это и есть «не скам» + коммерческая грамотность.

## Реализационные инварианты (на стороне ассистента, не груз пользователя)
- Разделить в контракте `casinoBank` (выплаты) и `faucetPool` (кран) — не полагаться на единый `address(this).balance`.
- Кран: защита от reentrancy (checks-effects-interactions / ReentrancyGuard), один claim на адрес.
- Верификация контракта: Etherscan API **V2** — ключ брать на **etherscan.io** (единый ключ работает и для Basescan), endpoint `https://api.etherscan.io/v2/api?chainid=84532`. V1 `api-sepolia.basescan.org` отключён 15.08.2025; сайт-эксплорер `sepolia.basescan.org` жив. (Прежняя запись про basescan.org устарела после миграции на V2.)
- Pyth Entropy на Base Sepolia: `0x41c9e39574F40Ad34c79f1C99B66A45eFB830d4c`. Паттерн V2: контракт наследует `IEntropyConsumer`, держит `IEntropyV2`, запрос `requestV2{value: getFeeV2()}()`, колбэк `entropyCallback(seq, provider, randomNumber)`. Адрес/RPC/ключи — в `contracts/.env.example`.

## Объём (зафиксирован)
Ядро (обязательно довезти, в порядке сборки):
1. Контракт: deposit/withdraw + кран + ставка Limbo с колбэком Pyth Entropy.
2. Деплой на Base Sepolia + верификация на Basescan.
3. Рабочий фронт: подключился → взял ETH из крана → поставил → увидел результат → вывел.
4. Дашборд самоаудита (live edge/RTP/hold + пруф каждой ставки).
5. Полировка: состояния, тексты, визуал.

Стретч (только если ядро твёрдо стоит): встроенный кошелёк + gasless; честный AI-штрих; вторая игра Dice.
В README «дальше»: всё недовезённое + roadmap.

## Деплой (Base Sepolia)
- **LimboCasino: `0x8B8d552C384685afaC6C01E6524A3053eb308e54`** — верифицирован на Basescan, owner `0x89AC49340E14A15cf48884706E60396C2345E119`. Засижено: банк 0.4 / кран 0.1 ETH.
- Колбэк Pyth проверен end-to-end on-chain (seq 55986: ставка 2x → выигрыш, payout начислен, инвариант учёта сошёлся). Адрес контракта → `web/.env.local` (`NEXT_PUBLIC_CASINO_ADDRESS`).
- **Фронт (live URL): https://limbo-casino-five.vercel.app** — задеплоен на Vercel (проект `limbo-casino`, Root Directory `web`), автодеплой Vercel↔GitHub на push в `main`. Прод-сборка Turbopack зелёная (фикс `tsconfig` target ES2020). Петля проверена на боевом URL on-chain (3 ставки + вывод).
- **Гейт безопасности slot 2 пройден** — 4 независимых adversarial-аудитора (Opus, read-only): CRITICAL/HIGH нет, edge честен (RTP макс 99%), средства защищены, газ колбэка с запасом 170%. Контракт сдаётся как есть, редеплой не нужен.

## Открытые вопросы (не зафиксировано)
- [ ] Честный AI-штрих — какой именно (объяснение честности / RG-нудж). Решаем, когда ядро готово.
- [ ] Брендинг и визуальная тема казино — на этапе полировки.
- [ ] Отдельный экран-верификатор (пересчёт прошлого результата из on-chain данных) — стретч прозрачности. **Приоритет повышен аудитом slot 2 (находка LOW): `BetSettled` не эмитит сырой `randomNumber`, поэтому пруф честности замыкается только через внешнее событие Pyth `Revealed` по `seq` + `previewMultiplier`. Верификатор на фронте закрывает нарратив «проверяемо on-chain» без редеплоя контракта.**
- [ ] README обязан раскрыть находки аудита slot 2 (принятые риски): trust-модель Pyth (`requestV2` без `userRandomNumber` → доверие честности валидатор+провайдер); force-fed ETH невозвратен by design; live-hold считается по всем ставкам (искажается рефандами).
